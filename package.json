{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "homepage": "https://github.com/Adelina980.github.io/touchTypingSimulator"
  },
  "scripts": {
    "predeploy": "rm -rf build && npm run build",
    "deploy": "gh-pages -d build"
  }
}

